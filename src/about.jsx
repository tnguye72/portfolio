import { Typography } from "@material-ui/core";
import * as React from "react"
import './main.css';

const About = () => {
    return (
        <div className="project-div">
            <Typography variant='h3' className="project-title">About Me</Typography>
            <Typography variant='h4' className="project-subtitle">***will be filled in a non-anonymous version***</Typography>
        </div>
    );
}

export default About;